// MOCK temporário da SDK do Asaas para testes locais
window.Asaas = {
    createCardToken: function (data, callback) {
      console.log("💡 MOCK: Gerando token...");
      setTimeout(() => {
        if (data.number === "5100000000001008") {
          callback({ id: "tok_test_12345" });
        } else {
          callback(null);
        }
      }, 1000);
    }
  };

  